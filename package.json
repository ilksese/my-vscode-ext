{
  "name": "my-vscode-ext",
  "version": "0.0.1",
  "private": true,
  "description": "VSCode extension monorepo",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "turbo": "^2.9.6",
    "typescript": "^5.5.0"
  }
}
